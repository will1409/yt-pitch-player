# Para rodar localmente, o Vite usa o proxy em vite.config.ts, então não precisa preencher isso.
# Na Vercel (Produção), defina esta variável apontando para o seu backend no Render:
# VITE_API_URL=https://seu-backend.onrender.com/api
